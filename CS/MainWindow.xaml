<Window x:Class="DXGridSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        xmlns:local="clr-namespace:DXGridSample"
        Name="win"
        Title="MainWindow"
        Width="525"
        Height="350">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="{x:Type dxg:ColumnChooserControl}">
            <Style.Resources>
                <Style TargetType="{x:Type dxg:GridColumnHeader}">
                    <Setter Property="local:ColumnChooserDoubleClickHelper.IsEnabled" Value="True"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <dxg:GridControl ItemsSource="{Binding Items}" >
        <dxg:GridControl.Columns>
            <dxg:GridColumn FieldName="Id"/>
            <dxg:GridColumn FieldName="Name"/>
        </dxg:GridControl.Columns>
    </dxg:GridControl>

</Window>
